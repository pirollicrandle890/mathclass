// Example 1: A simple example of an asynchronous function
async function calculateArea() {
    const width = 10;
    const height = 5;
    let area = width * height;
    console.log(`The area is ${area}`);
}
calculateArea();
